import {
  createComment,
  likeComment,
  deleteComment,
  updateComment,
} from './CommentsResolver';

export const CommentsQuerys = {};

export const CommentsMutations = {
  createComment,
  likeComment,
  deleteComment,
  updateComment,
};
