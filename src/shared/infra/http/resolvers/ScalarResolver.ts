import { resolvers } from 'graphql-scalars';

const {
  ObjectID,
  Date,
  Time,
  DateTime,
  EmailAddress,
  PhoneNumber,
  URL,
  UUID,
  GUID,
  Port,
  Currency,
  Void,
} = resolvers;

export default {
  ObjectID,
  Date,
  Time,
  DateTime,
  EmailAddress,
  PhoneNumber,
  URL,
  UUID,
  GUID,
  Port,
  Currency,
  Void,
};
